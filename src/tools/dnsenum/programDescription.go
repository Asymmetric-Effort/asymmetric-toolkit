package main

const (
	Description = `This is a DNS enumeration tool which uses either a sequential, random or dictionary source
to discover and enumerate the specified available DNS records on a given domain.`
)
